import { styled } from "@mui/material";
import { Box } from "@mui/material";

export const SearchLayout = styled(Box)(() => ({
  position: "relative",

  width: "100%",
  height: "100%",

  minHeight: "100vh",
}));
